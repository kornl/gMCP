/**
 * I really would like to have the src directory with the Makevars file to build the jar file.
 * But how can I disable the WARNING "Subdirectory 'src' contains no source files."
 * other than creating such a dummy c file like this? Or where should I put the java-src?
 */
